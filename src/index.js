import React from 'react';
import ReactDOM from 'react-dom/client'; // Note the updated import for 'react-dom/client'
import App from './App.jsx';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
